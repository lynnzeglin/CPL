<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:background="@android:color/white"
    android:padding="1dp">

    <ImageView
        android:id="@+id/library_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:paddingEnd="24dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:contentDescription="@string/desc_library_image"
        android:src="@drawable/ic_tree_library"
        />

    <TextView
        android:id="@+id/library_address"
        android:layout_width="fill_parent"
        android:layout_height="26sp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@id/library_icon"
        android:ellipsize="marquee"
        android:maxLines="1"
        android:textSize="12sp"
        />

    <TextView
        android:id="@+id/library_name"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/library_address"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toEndOf="@id/library_icon"
        android:gravity="center_vertical"
        android:textSize="16sp"
        />

</RelativeLayout>